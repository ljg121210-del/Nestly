import { AppShell } from "@/components/AppShell";import { DashboardHeader } from "@/components/dashboard/DashboardHeader";import { JobFeed } from "@/components/dashboard/JobFeed";import { QuoteForm } from "@/components/forms/QuoteForm";export default function ProviderJobsFeedPage({searchParams}:{searchParams:{job?:string}}){return <AppShell role="provider"><DashboardHeader eyebrow="Available jobs feed" title="Browse open jobs and quote fast." subtitle="Manual browsing improves coverage so jobs do not go cold."/><div className="grid gap-5 xl:grid-cols-[1fr_380px]"><JobFeed providerView/><aside>{searchParams.job?<QuoteForm jobId={searchParams.job}/>:<div className="rounded-[2rem] bg-white p-6 shadow-premium"><h3 className="text-2xl font-black">Select a job</h3><p className="mt-2 text-sm text-nestly-muted">Tap a job card to open the quick quote form.</p></div>}</aside></div></AppShell>}
