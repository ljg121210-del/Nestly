import { AppShell } from "@/components/AppShell";import { DashboardHeader } from "@/components/dashboard/DashboardHeader";import { EmptyState } from "@/components/dashboard/EmptyState";import { Button } from "@/components/ui/Button";export default function ProfilePage(){return <AppShell role="provider"><DashboardHeader eyebrow="Profile" title="Logo, profile and trust setup." subtitle="Replace the logo file in /public/logo and connect Supabase profile data."/><EmptyState title="Add your real Nestly logo" body="Drop your logo into public/logo and update components/Logo.tsx if needed." action={<Button href="/provider/onboarding">Provider onboarding</Button>}/></AppShell>}
