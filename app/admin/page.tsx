import { AppShell } from "@/components/AppShell";import { DashboardHeader } from "@/components/dashboard/DashboardHeader";import { MetricCard } from "@/components/dashboard/MetricCard";import { BriefcaseBusiness,CreditCard,ShieldCheck,Users } from "lucide-react";export default function AdminPage(){return <AppShell role="admin"><DashboardHeader eyebrow="Admin console" title="Marketplace control centre." subtitle="MVP admin shell for users, jobs, payments and verification queue."/><div className="grid gap-4 md:grid-cols-4"><MetricCard icon={Users} label="Users" value="Live" sub="Supabase users"/><MetricCard icon={BriefcaseBusiness} label="Jobs" value="Open" sub="Marketplace jobs"/><MetricCard icon={CreditCard} label="Payments" value="Stripe" sub="Checkout/session tracking"/><MetricCard icon={ShieldCheck} label="Verification" value="Queue" sub="Provider checks"/></div><div className="mt-6 rounded-[2rem] bg-white p-6 shadow-premium"><h2 className="text-2xl font-black">Admin next steps</h2><p className="mt-2 text-nestly-muted">Add protected admin auth before public launch. This is the management shell for provider approvals, disputes and payment reviews.</p></div></AppShell>}
